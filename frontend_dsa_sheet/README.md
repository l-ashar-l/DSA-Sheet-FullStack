# React App

This project is now configured as a Create React App frontend using `react-scripts`.

Available scripts:

- `npm start` — start the development server
- `npm run build` — build the production bundle
- `npm test` — run tests
- `npm run eject` — eject CRA configuration

The app uses React Router and Axios for client-side navigation and backend API calls.
